# SASS directory & guidelines

The `main.scss` file is the parent file that *'imports'* (`@use`) all the other Sass partials.

It's compiled into a `main.css` file that's then used in this web solution.
