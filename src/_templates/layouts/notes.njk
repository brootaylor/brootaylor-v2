{% extends "layouts/base.njk" %}

{% block content %}
	<article class="[ wrapper flow ] note">

		<h1>{{ title }}</h1>
		<time datetime="{{ date | dateLocal }}">{{ date | dateLocalFriendly("d LLLL y, ") }}{{ date | dateLocalFriendly("t") | lower }}</time>

		{{ content | safe }}

	</article>
{% endblock %}
