{% extends "layouts/base.njk" %}

{% block content %}
	<article class="[ wrapper flow ] article">

		<h1>{{ title }}</h1>
		<time datetime="{{ date | dateLocal }}">{{ date | dateLocalFriendly("LLLL d, y") }}</time>

		{{ content | safe }}

	</article>
{% endblock %}
